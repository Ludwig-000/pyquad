use macroquad::models::Mesh;

/// idk yet. some kind of batching function that draws all internal objects while applying any shaders.
/// 
use crate::engine::SHADERS::shader_manager::*;



fn batch_draw_all_3d_objects(){
    let gl = unsafe  {macroquad::prelude::get_internal_gl() };
 
    todo!();

}
